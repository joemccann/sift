# Autoresearch Ideas

## Promising Next Features
- Transcript item tagging — add string tags/labels to items
- Markdown code block detection — detect ```sql blocks in responses 
- Source alias — user-defined short names for sources
- Query result formatting — detect column types and format numbers/dates
- DuckDB "join [table1] and [table2]" — cross-table join pattern
- Parquet row group info — parquet_file_metadata for detailed file info
- Compact transcript mode — show only user messages and results
- Import from URL — accept http:// URLs for remote parquet/CSV files

## Completed (44 experiments, 450 tests)
All backlog items, 20 slash commands, 4 file formats, 15+ DuckDB patterns,
tab completion, bookmarks, pinning, query templates, batch import,
source validation/grouping/sorting/search, transcript pagination/export/summary,
appearance settings, session export, comprehensive error handling.
