# Autoresearch Ideas

## Promising Next Features
- Query result formatting — detect column types and format numbers/dates
- DuckDB "join [table1] and [table2]" — cross-table join pattern  
- Import from URL — accept http:// URLs for remote parquet/CSV files
- Parquet row group info — parquet_file_metadata for detailed file info
- Source comparison — compare schemas of two sources
- Query execution timing — track and display per-query timing stats
- Transcript word count / character count computed properties
- DuckDB "top N by column" — ORDER BY with LIMIT pattern
- Provider response caching — cache recent provider responses
- Bulk tag operations — tag multiple items at once

## Completed (50 experiments, 509 tests)
All backlog items + 22 slash commands, 4 file formats, 15+ DuckDB patterns,
tab completion, bookmarks, pinning, tagging, query templates, batch import,
source aliases/validation/grouping/sorting/search, transcript pagination/export/summary/compact,
appearance settings, session export, markdown detection, error handling, query builders.
