# Autoresearch Ideas

## Promising Next Features
- DuckDB "join [table1] and [table2]" — cross-table join pattern
- Import from URL — accept http:// URLs for remote parquet/CSV files
- Source comparison — compare schemas of two sources
- Provider response caching — LRU cache for recent responses
- Bulk tag operations — tag multiple items matching a query
- Result row count parsing — extract row counts from DuckDB output
- Transcript diffing — compare two transcript items
- DuckDB "where [condition]" — filter pattern for queries
- Smart prompt suggestions — suggest queries based on source kind + history

## Completed (54 experiments, 550 tests)
Everything from prior sessions plus: execution stats tracking, transcript analytics,
DuckDB top-N-by-column, summarize/describe query builders, tabular/db source filtering,
composer command detection, comprehensive planner pattern coverage.
